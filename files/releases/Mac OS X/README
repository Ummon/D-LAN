Put here the released setup. The format must be (EBNF) :

format = "D-LAN-", version, version tag, "-", year, "-", month, "-", day, "_", hour, "-", minute, ".dmg" ;
version = digit, ".", digit, ".", digit ;
version tag = { alphabetic character } ;
year = digit, digit, digit, digit ;
month = digit, digit ;
day = digit, digit ;
hour = digit, digit;
minute = digit, digit;

The filename is parsed to print some information on the web page.
